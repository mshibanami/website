box: newsdev/rails

build:
  steps:
    - script:
      name: Prepare the build
      code: ./build

deploy:
  steps:
    - lukevivier/gh-pages:
      token: $GITHUB_TOKEN
      repo: mshibanami/mshibanami.github.io
      basedir: output
