box: newsdev/rails

build:
  steps:
    - script:
      name: Prepare the build
      code: ./build

deploy:
  steps:
    - lukevivier/gh-pages@0.2.4:
      token: $GITHUB_TOKEN
      repo: mshibanami/mshibanami.github.io
      basedir: output
